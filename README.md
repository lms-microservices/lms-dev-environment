# LMS Dev Environment
## Setup
1\. Clone this repo

2\. Clone all microservices inside this folder (or use submodules)

3\. Copy `.env.example` to `.env` and fill in values

## Run Locally

```bash
docker compose up --build
```

